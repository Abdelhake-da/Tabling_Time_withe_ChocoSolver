# Timetabling with ChocoSolver

University timetabling (emploi du temps) solver using the ChocoSolver constraint programming library.

## Tech
- Java
- ChocoSolver (constraint programming)
- `lib/` — dependencies, `data.json` — input data

Companion UI: `Interface_TablingTime` (JavaFX).
